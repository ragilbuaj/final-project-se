PSEUDOCODE PROGRAM LIBRARY APP

READ AND WRITE books with LIST
READ AND WRITE funType with STRING
READ AND WRITE borrowBook with LIST
READ AND WRITE returnedBook with LIST

IF funType = "borrow" {
	READ AND WRITE isActive with BOOLEAN
	
	STORE isActive with true
	STORE borrowBook with "selected" book from books list
	
	WHILE (isActive = true) 
	DO{
		IF book.count != 0 {
			ADD "selected" book to borrowBook list
		}
		ELSE {
			REMOVE book from borrowBook list
		}
	}
	PRINT (borrowBook)
}
ELSEIF funType = "return" {
	READ borrowBook
	READ AND WRITE isActive with BOOLEAN
	
	STORE isActive with true
	STORE returnedBook with "selected" book from borrowBook list

	WHILE (isActive = true)
	DO {
		ADD "selected" book from borrowBook to returnedBook list
	}
	
	REMOVE "selected" book from borrowBook list

	PRINT (returnedBook)
}
ELSE {
PRINT ("ERROR : NOT IN THE OPTIONS")
}